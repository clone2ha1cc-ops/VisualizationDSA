/**
 * @deprecated
 * File này giữ lại để backward compatibility.
 * Store chính đã được di chuyển vào:
 *   src/features/vcr-player/store/useVcrStore.ts
 *
 * Sử dụng `useVcrStore` từ '@/features/vcr-player' thay thế.
 */
export { useVcrStore as usePlaybackStore } from '../features/vcr-player/store/useVcrStore';
